def perkalian(a, b):
    hasil = 0
    for _ in range(n):  # Ulang sebanyak 'n' kali
        hasil += b  # Menambahkan 'b' ke hasil
    return hasil

# Test Case
x, y = 6, 5
print(f"{x} x {y} = {' + '.join([str(y)] * x)} = {perkalian(x, y)}")

x, y = 7, 10
print(f"{x} x {y} = {' + '.join([str(y)] * x)} = {perkalian(x, x)}")
